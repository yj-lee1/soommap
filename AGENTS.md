# 숨맵 작업 규칙

사용자가 2026-09-17 요청한 작업 방식이다. 이후 사용자의 명시적 변경이 있으면 그 지시를 우선한다.

- 대상 원격은 `git@github.com:yj-lee1/soommap.git`이다. 연결 상태·기존 이력을 확인하고 로컬 문서와 기존 사용자 변경을 보존한다.
- 작업 순서와 범위는 `docs/soommap-execution-plan.md`, 현재 단계와 승인 기록은 `docs/soommap-progress.md`를 먼저 확인한다.
- Codex가 단독으로 작업한다. 다른 개발자나 별도 에이전트에 작업을 분담하지 않는다.
- 승인된 단계 안에서는 구현·수정·검증을 자율적으로 진행한다. 사람의 판단·계정 접근·미승인 비용·범위 변경이 필요하면 구체적인 이유와 선택지를 제시한다.
- 각 단계가 끝나면 결과물과 실제 수행한 검증, 확인 방법, 남은 제한을 보고하고 사용자 검수를 요청한다. 명시적인 승인 전에는 다음 단계 구현을 시작하지 않는다. 시간 경과를 승인으로 처리하지 않는다.
- 수정 의견은 같은 단계에서 반영하고 재검수한다. 진행·검수·승인 상태를 기록한다.
- 기능을 먼저 완성하고 시각 디자인은 뒤에서 일괄 적용한다. 기본 모바일 조작·조건 의미·로딩·오류·데이터 출처는 기능 단계에 포함한다.
- 기존 제품 기획·수상 검토·확장 보완 문서를 보존한다. 제품 범위를 바꿀 때 관련 계획과 사용자 승인 내용을 명확히 남긴다.
- 키를 채팅·문서·로그·Git에 노출하지 않는다. 사용자에게는 필요한 설정 위치와 변수 이름을 안내한다.
- 원격 반영과 검수 배포의 실제 상태·버전을 구분해 보고한다. 최종 기본 브랜치 반영·배포는 완성본 검수에서 승인받은 범위로 진행한다.
- 외부 사람에게 연락하거나 참가 약관 동의·최종 제출을 대신 수행하려면 해당 행동에 대한 사용자 지시가 있어야 한다.
- 2026-09-17 추가 승인: 0단계 검수 후 1단계 착수. 이후 브랜치는 `feat/hangang-data-foundation` 같은 일반적인 `feat/`, `fix/`, `docs/` 이름을 사용한다.
- Vercel Hobby와 표준 Next.js를 유지한다. 추가 유료 기능·플랜은 사전 보고한다. 장시간/상시 실행 작업에 의존하지 않는다. Preview/Production 키를 분리하고 검증된 인프라를 불필요하게 재생성·재검사하지 않는다.
- 서울시·OpenAI 호출은 서버 전용. 캐시·동시 요청 병합·타임아웃·오류 대체를 적용하고 키를 클라이언트·로그·Git에 노출하지 않는다.
- OpenAI 기본 모델은 `gpt-5.6-terra`, 개발·검수 합산 총 $20. 정확도를 우선하며 Structured Outputs, 제한된 문맥·출력, 흐름당 조건 해석→설명 최대 2회를 원칙으로 한다. 버튼 조정은 일반 코드로 처리한다. 비싼 모델 자동 fallback·별도 AI 서비스 추가는 승인 전 금지한다.
- 사용자 입력 캐시는 다른 사용자에게 섞이지 않아야 한다. 반복 개발 검사는 fixture/mock을 기본으로 하고 실제 API 검사는 필요한 범위만 수행한다. 누적 예상 비용과 예약액을 기록하고 한도 근처에서는 비필수 호출 전에 보고한다.
- 우선순위: 핵심 흐름 정확성·안정성 → 데이터/AI 신뢰성 → 모바일·속도 → 사용량 → 부가 기능. 무료 한도 때문에 핵심 품질을 줄여야 하면 현재 제약·예상 추가 비용·대안을 먼저 보고한다.
- 2026-09-17: 1단계 검수 승인 및 2단계 착수 지시. 31분/34분 표시는 각각 확인 시각 15:26/15:29에서 원자료 14:55를 뺀 정상 계산임을 화면에서 확인했다.
- 2026-09-17: 사용자 도착시각·혼잡예측 정렬 보완 요청에 따라 2단계를 확장한다. 같은 스냅샷의 인접 인구 범위만 숨맵 추정으로 선형 보간하고, 혼잡 단계는 보간하지 않는다. 체류 구간·누락·선호 불확실성을 코드로 평가하고 원시 근거를 남긴다. TMAP·추가 유료 API·3단계 AI 구현 승인을 대신하지 않는다. 상세 정책은 `docs/temporal-alignment.md`를 따른다.
- 2026-09-17 후속: 사용자가 최종 확정 후 네이버·카카오 대중교통 길찾기, 출발지 재사용, displayCoordinate/accessPoint 분리를 요청하고 단계 조정 후 작업 재개를 지시했다. 이를 2단계 보완 후 다음 작업인 3단계 착수 지시로 기록한다. 이동 계산·외부 지도 실행은 `docs/mobility-plan.md`에 따라 4.5단계로 추가하며, 단계별 검수와 별도 유료 API 한도 확인은 유지한다.
- 2026-09-17: 사용자가 Upstash Redis Free 사용을 승인했다. 결제수단·자동 유료 전환 없이 공유 AI 예산의 사전 예약/정산, 사용자·IP rate limit, 중복 방지와 짧은 TTL 캐시를 같은 Redis에서 처리한다. Supabase와 비교 후 선택한 이유는 `docs/ai-runtime.md`에 남긴다.
- 2026-09-17: 3단계 검수 보고 이후 사용자가 “다음단계 진행해.”로 4단계 착수를 승인했다. 고정·해제·제외·정정·계획 확정과 선택 자료 재확인을 구현한다. 4.5단계 TMAP·최종 길찾기는 별도 단계 검수 규칙을 유지한다.

- 2026-09-17: 4단계 보고 이후 “다음 단계 진행해”로 4.5단계 이동시간·길찾기 착수 승인. TMAP 유료 전환은 별도 승인 전 적용하지 않는다.

- 2026-09-17: 사용자가 TMAP 개발·검수 종량제 총 5,000원을 승인했다. OpenAI $20과 별도 관리. 계정에서 종량제 신청·키 설정을 마친 뒤 paid 모드를 사용하며 서버에서 요청 전 atomic 예약으로 총액을 제한한다.

- 2026-09-17 후속: 사용자가 TMAP 키 설정 완료 및 “우선 Free로 연결하고 종량제는 나중에”를 선택했다. 현재 로컬·Preview는 free, 하루 10건을 유지한다. 과거 5,000원 예산 승인만으로 종량제를 켜지 않고 추후 사용자 전환 지시를 기다린다.

- 2026-09-17: 사용자가 네이버지도 길찾기 연결 성공을 확인하고 5단계 진행을 승인했다. 재방문 시 내용 보존 요청을 5단계 우선 기능으로 반영한다. 같은 브라우저에 입력·출발지·조정·선택 계획을 저장하고, 오래된 혼잡/이동시간은 현재 정보로 복원하지 않는다. 카카오 실기기 검증 완료를 의미하지 않는다.

- 5단계부터 고정 검수 주소 `https://soommap-review-yeongjis-projects-f12fc56b.vercel.app`를 사용한다. 새 Preview 검증 후 이 alias를 갱신해 같은 origin의 브라우저 계획 저장을 유지한다. Production 승인을 의미하지 않는다.

- 2026-09-18: 사용자가 6단계 준비를 지시했다. 5단계 완료를 전체 기능 검수 완료로 간주하지 않는다. 최신 기획/1~5단계의 핵심 흐름을 실제 연동·모의·미구현·실기기 미검증으로 나누고, 차단급 문제만 먼저 수정한다. 시작/추천·조건 수정/선택·길찾기의 모바일·데스크톱 및 실패 상태 구성안을 먼저 검수받은 뒤 전체 UI를 변경한다. 디자인 후 같은 핵심 흐름과 첫 사용자 이해도를 재검수한다. 상세 기록은 `docs/reviews/core-flow-pre-design.md`, `docs/design/stage-6-screen-plan.md`를 따른다.

- 2026-09-19: 사용자가 통합 검수 결과와 3구역 화면 흐름을 디자인 작업 방향으로 승인했다. 최종 디자인·기능 전체·출시 승인이 아니다. 전체 적용 전에 같은 모바일 추천 화면의 두 고충실도 시각 방향과 세 동작 샘플을 제시하고 확인받는다. Design Spells 실제 3개 사례를 목적에 맞게 변형하며, 고정 유지·시간축 탐색·실행 카드 전환을 우선한다. 공식/가공/관측/예측/공백을 구분하고 접근성·reduced-motion을 검수한다. 상세는 `docs/design/stage-6-visual-directions.md`. 실제 기기·첫 사용자 검증과 공개 TMAP Free 한도는 미완료로 유지한다.

- 2026-09-19 후속: 사용자가 A ‘서울 산책 안내서’를 선택하고 전체 디자인 통합·최종 검수·제출 수준 완성 및 원티드 제출본 작성·임시저장을 연속 수행하도록 지시했다. 이번 6~8단계는 별도 단계 승인 대기 없이 보고하며 계속한다. 최종 과제 제출 버튼은 누르지 않는다. 실제 기기·첫 사용자 검증을 허위 완료로 바꾸지 않는다. TMAP 개발·검수·공개 체험 합산 종량제 총 5,000원 승인; 계정 상품 전환 완료 확인 전 free 유지.

- 2026-09-19 최종 비용 선택: 사용자가 “그냥 free로 일단 하자”로 TMAP 종량제 전환을 다시 보류했다. 현재 Free 하루 10건을 유지한다. 유료 예산 승인 기록만으로 paid를 켜지 않으며 공개 소진 시 입력·출발지를 보존한 수동 방문 시간대 비교로 안내한다.

- 2026-09-19: 사용자가 A안의 정보 배치 개선·선명한 색/지도·조건 고정/시간축/실행 카드 모션 강화를 승인하고 완료 후 새 대표이미지와 스크린샷 채팅 첨부를 요청했다. 기존 추천/API/예산 계약을 보존하며 개선·검수·고정 Preview 갱신까지 진행한다. 제출 이미지 업로드는 사용자 담당, 최종 제출 미실행 유지.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
